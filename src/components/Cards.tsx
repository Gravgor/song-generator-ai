"use client";
import styled from "@emotion/styled";
import { Card } from "@mui/material";



export const SongIdeaCard = styled(Card)({
   width: '920px',
    backgroundColor: '#fff',
    boxShadow: '0 4px 8px rgba(0, 0, 0, 0.1)',
    borderRadius: '8px',
    padding: '2rem',
    marginBottom: '2rem',
  });

export const AIResponseCard = styled(Card)({
  backgroundColor: '#fff',
  boxShadow: '0 4px 8px rgba(0, 0, 0, 0.1)',
  borderRadius: '8px',
  padding: '2rem',
  marginBottom: '2rem',
});
